# To create or update a token visit https://envio.dev/app/api-tokens
ENVIO_API_TOKEN="<YOUR-API-TOKEN>"

# Optional: Dedicated IPFS gateway URL (e.g. Filebase, Pinata)
# Falls back to public gateways if not set
IPFS_GATEWAY_URL=""
